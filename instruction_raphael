# After the comment, the inscription is written. Each command from a new site.
# List of commands:
# forward
# right
# left
# is_wall (used for a condition that says whether there is a wall here)
# shoot
# is_see_tank (used for the condition that says whether there is a tank ahead)
# random (randomly right or left)
# "action" if "condition" else "action"
shoot
shoot if is_see_tank else forward
shoot if is_see_tank else forward
shoot if is_see_tank else forward
shoot if is_see_tank else forward
shoot if is_see_tank else forward
shoot if is_see_tank else forward
left if is_wall else forward
shoot if is_see_tank else forward
shoot if is_see_tank else forward
shoot if is_see_tank else forward
shoot if is_see_tank else forward
shoot if is_see_tank else forward
shoot if is_see_tank else forward
random